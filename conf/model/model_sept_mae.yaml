name: model_sept_mae
phase: forecast

target:
  _target_: src.model.trainer_sept_mae.Trainer
  dim: 256
  historical_steps: 50
  future_steps: 60
  tempo_depth: 3
  roadnet_depth: 3
  spa_depth: 2
  num_heads: 8
  mlp_ratio: 4.0
  qkv_bias: False
  drop_path: 0.2
  pretrained_weights: ${pretrained_weights}
  lr: ${lr}
  weight_decay: ${weight_decay}
  epochs: ${epochs}
  warmup_epochs: ${warmup_epochs}
  post_norm: ${post_norm}
  road_embed_type: ${road_embed_type}
  tempo_embed_type: ${tempo_embed_type}
